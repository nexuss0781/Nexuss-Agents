# Generated by Anybuild v0.29.0 — https://anybuild.run
# This file is yours to edit: add steps or override the serve below.

load("//anybuild/tools:node.bzl", "node_build", "node_config", "node_serve")
load("//anybuild:services.bzl", "postgres")

config = node_config(
    schema = 1,
    commands = {
        "start": "NODE_ENV=production node dist/index.js",
    },
    services = [postgres()],
    node_package_manager = "pnpm",
    node_server = "express",
    node_build_command = "pnpm run build",
    node_version = "24",
    pnpm_version = "10.4.1",
)

build = node_build(config)

node_serve(config, build, name = "Nexuss-Agents")
